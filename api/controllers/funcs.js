'use strict';

module.exports = function() {
    return {
        /*
         * GET the JSON object from the end point
         */
        get(url) {

        }

        /*
         * Operate on the user latitude and longitude
         */
        haversine(lat, long) {

        }

        /*
         * POST the JSON object or the errors
         */
        post(userArray) {
            
        }
    }
}